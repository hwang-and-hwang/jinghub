import React from 'react';
import Header from '../_components/Header/Header';

type MainProps = {
  
};

const Main: React.FC<MainProps> = ({}) => {
  return (
    <div>
      <Header></Header>
    </div>
  );
};

export default Main;